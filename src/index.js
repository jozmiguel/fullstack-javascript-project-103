export default function genDiff(filepath1, filepath2) {
  // Por ahora devolvemos cadena vacía — las pruebas/steps iniciales solo
  // necesitan que la función exista y sea exportada por defecto.
  return '';
}
